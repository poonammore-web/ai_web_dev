# AI Web Dev Starter

Bootstrap + HTML/CSS/JavaScript starter scaffold.

Getting started

- Open `index.html` in a browser, or run a lightweight dev server.

Options:

- Install dev server and run:

```bash
npm install
npm start
```
